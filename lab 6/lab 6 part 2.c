#include <stdio.h>
int max_of_3 ( int x, int y, int z)
 
 int larger, largest;
	if (x > y)
	{
		larger = x;
	}
		else
	{
		larger = y;
	}
		
		if (larger > z)
	{
		largest = larger;
	}
		else
	{
		largest = z;
	}
 

